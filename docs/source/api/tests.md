---

## Tests

**Path:** `docs/source/api/Tests.md`

```markdown
# Tests Package

Directory with test results, separated by date. Each date gets a test_logbook with an overview of all tests run and their outcome. Failed tests create a dataset to review the problematic datapoints. 

```{autosummary}
:toctree: _autosummary
:recursive:

tests